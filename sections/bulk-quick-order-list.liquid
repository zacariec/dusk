{% render 'vite-tag' with 'quick-order-list.css'' %}
{% render 'vite-tag' with 'quick-order-list.ts'' %}

{% render 'quick-order-list', product: product, show_image: true, show_sku: true, is_modal: true %}

{% schema %}
{
  "name": "t:sections.quick-order-list.name",
  "limit": 1,
  "enabled_on": {
    "templates": ["product"]
  }
}
{% endschema %}
